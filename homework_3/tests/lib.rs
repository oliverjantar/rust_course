/*
use std::io::Cursor;

use lesson_2::run;

#[test]
fn should_return_result_with_formated_text() {
    let args = vec!["".to_string(), "lowercase".to_string()];

    let line = Cursor::new("Lorem Ipsum Dolor Sit Amet");

    let result = run(line, std::io::stdout(), &args);

    assert!(result.is_ok());
    assert_eq!(result.unwrap(), "lorem ipsum dolor sit amet");
}
*/
